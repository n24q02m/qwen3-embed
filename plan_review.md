Need to handle PR comments. Will use a tool `read_pr_comments` if available. Oh wait, I don't have that tool explicitly documented but the system prompt said "read these using `read_pr_comments`".

1. Call `read_pr_comments` tool to see what feedback was left.
2. Formulate a plan based on the feedback.
3. Apply changes and verify.
4. Resubmit.
