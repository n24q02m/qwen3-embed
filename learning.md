Avoid assertions in finally blocks in parallel processing code as they can mask original exceptions.
Be careful when using sed to replace variable names, it might inadvertently affect other lines or context. Always double check context.
