## 2025-06-16 - No UI found in repo
**Learning:** This repository (`qwen3-embed`) is a pure backend Python library for text embeddings and reranking with no frontend or user interface components.
**Action:** When acting as the Palette agent, abort UI-specific tasks, document the absence of UI, and yield an empty patch without creating a PR.
